import { styled } from "styled-components";

const Pagination = () => {
  return (
    <div>
      <h1>Pagination</h1>
    </div>
  );
};

const Paginationstyled = styled.div``;

export default Pagination;
